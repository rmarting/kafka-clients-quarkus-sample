apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kafka-clients-quarkus-sample-chart.fullname" . }}-test-connection"
  labels:
    {{- include "kafka-clients-quarkus-sample-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kafka-clients-quarkus-sample-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
